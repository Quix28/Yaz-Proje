__pycache__/
*.pyc
.DS_Store

# Agent/IDE config — local only, not for version control
.agents/
agent/
.claude/
skills-lock.json

# training run artifacts — regenerated per-run, not deliverables
*.resume
train.log
wandb/
