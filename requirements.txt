torch==2.12.1
casadi==3.7.2
numpy==1.24.3
scipy==1.10.1        # qmc sampling; solve_discrete_are (LQR). pinned: newer scipy
                     # forces numpy>=1.25/2.x which breaks the casadi/numpy 1.24.3 stack
matplotlib>=3.7,<4.0  # eval plots, already used by visualize.py
tqdm>=4.65            # dataset-gen / training progress
